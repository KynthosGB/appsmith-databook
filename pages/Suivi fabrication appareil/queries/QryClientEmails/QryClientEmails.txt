SELECT
  id,
  numero_affaire,
  numero_appareil,
  sent_at,
  received_at,
  from_email,
  subject,
  body_preview,
  body_html,
	body_html_resolved_b64,
  mailbox
FROM public.client_emails
WHERE
  numero_appareil = {{ appsmith.URL.queryParams.numero_appareil }}
ORDER BY received_at DESC NULLS LAST, sent_at DESC NULLS LAST
LIMIT 200;
