UPDATE public.workflow_fabrication
SET
  date_debut = {{ this.params.date_debut }}::date,
  date_fin   = {{ this.params.date_fin }}::date
WHERE groupe_appareil_id = {{ this.params.groupe_appareil_id }};